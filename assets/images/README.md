This folder is for Aggie's photography. Replace the placeholder Unsplash URLs in `index.html` and `data/flavors.json` with local image paths here (e.g. `assets/images/icecream1.jpg`).

Tips:
- Use large, high-quality photos sized for web (1200–2000px wide) and compress for fast load times.
- Keep original files in a separate `raw/` folder outside the webroot for archival.
